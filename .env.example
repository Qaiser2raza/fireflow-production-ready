# ============================================================
# Fireflow Restaurant System - Environment Configuration
# ============================================================
# This file is for REFERENCE ONLY. Copy to .env.local to use

# ============================================================
# DATABASE CONFIGURATION (PostgreSQL Local)
# ============================================================
# The Express server (server.ts) connects to PostgreSQL
# These values are HARDCODED in server.ts but can be overridden

DATABASE_USER=postgres
DATABASE_PASSWORD=admin123
DATABASE_HOST=localhost
DATABASE_NAME=fireflow_local
DATABASE_PORT=5432

# Connection String (for Prisma)
DATABASE_URL="postgresql://postgres:admin123@localhost:5432/fireflow_local?schema=public"

# ============================================================
# CLOUD SUPABASE CONFIGURATION (SaaS Operations Only)
# ============================================================
# SEPARATE from local database - used for:
# - License key management
# - Subscription tracking
# - Payment verification
# 
# Set these only if you want cloud SaaS features
# Leave blank to run in local-only mode

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================
# SERVER CONFIGURATION
# ============================================================
NODE_ENV=development
SERVER_PORT=3001
SERVER_HOST=localhost

# ============================================================
# VITE/FRONTEND CONFIGURATION
# ============================================================
# These are automatically resolved since:
# - Frontend runs on http://localhost:5173 (Vite default)
# - Server runs on http://localhost:3001
# 
# Supabase is OPTIONAL and only needed for cloud SaaS features
# Operations work fine in local-only mode

# Override server URL if running on different host
# VITE_LOCAL_API_URL=http://192.168.x.x:3001/api
# VITE_LOCAL_WS_URL=ws://192.168.x.x:3001

# ============================================================
# DEPRECATED (NO LONGER USED)
# ============================================================
# Legacy Supabase config (if found in old .env files, remove it)
# We now have separate cloud client for optional SaaS features

# ============================================================
# OPTIONAL: EXTERNAL INTEGRATIONS
# ============================================================

# Google Gemini AI
# VITE_GEMINI_API_KEY=your_gemini_api_key

# Google Maps API (for delivery route optimization)
# VITE_GOOGLE_MAPS_API_KEY=your_api_key

# ============================================================
# LOGGING & DEBUG
# ============================================================
DEBUG=fireflow:*
LOG_LEVEL=info
# ============================================================
# NOTIFICATION CONFIGURATION
# ============================================================
NOTIFICATION_ENABLED=true
NOTIFICATION_WEBHOOK_URL=your_whatsapp_or_sms_webhook_url

# ============================================================
# SUPABASE SERVER-SIDE (for subscription checker job)
# ============================================================
# Service key (NOT anon key) for server-side writes to Supabase
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_supabase_service_role_key